"use strict";

/**
 * Returns whether or not the value is a number-like value
 * @param {string|number} v Number-like value
 * @returns {boolean}
 */
function isNumber(v) {
    return /^[+-]?(\d+\.?|\d*\.\d+)$/.test(v);
};

// Exports
module.exports = isNumber;