"use strict";

/**
 * Returns if a number-like value is zero
 * @param {string|number} v Number-like value
 * @returns {boolean}
 */
function isZero(v) {
    return /^[+-]?(0+\.?|0*\.0+)$/.test(`${v}`);
};

// Exports
module.exports = isZero;